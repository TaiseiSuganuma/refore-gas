# 案件マスタ / 関連シート スキーマ

書類生成の入力源となるスプレッドシートの構造。

**2026-05-27 改訂**: 旧「1案件=1スプレッドシート」方式から **「1案件=1行+1フォルダ」の集約型** に変更。
本書は新方針の最終形を記載。旧 § 7「案件サマリーシート」概念は廃止された（[`specification.md` § 12](./specification.md#12-設計変更履歴) 参照）。

- **対象スプレッドシート**: [【リフォレ】書類自動生成](https://docs.google.com/spreadsheets/d/12BEUxTqnADqWsmffGWos0tFe08FohEE-0RQJbLNAnEk/edit) (fileId: `12BEUxTqnADqWsmffGWos0tFe08FohEE-0RQJbLNAnEk`)
  - Phase 4 で Google フォーム連携時、別ファイルにマイグレーションする可能性あり
- 案件マスタ自体は **このスプレッドシート内の「案件一覧」シート → 「案件マスタ」シート** に拡張する形（Phase 2 で改名・列拡張）

---

## 1. シート構成

| シート名 | 役割 | 状態 |
|---|---|---|
| 案件一覧 → 案件マスタ | **1 案件 1 行で全情報を集約** | Phase 1 では「案件一覧」のまま、Phase 2 で「案件マスタ」に改名・列拡張 |
| 物件 | 案件 ID で紐づく物件・樹種別の明細行（繰返し項目） | 既存「明細」シートを Phase 1 で「物件」に再設計 |
| 設定 | 出力先フォルダ ID、テンプレシート名、書類マスタシート名等 | 既存 |
| 書類マスタ | 書類ごとのテンプレ ID・対応パターン・命名規則 | **Phase 2 で新設** |
| 見積書 | 見積書のレイアウトシート（PDF 元データ） | 既存。Phase 6 で document-generator に統合 |

---

## 2. 「案件マスタ」シート（旧「案件一覧」）

### Phase 1 段階（現状の案件一覧シート）

土地売買契約書 MVP の入力として最低限必要な列。詳細は [`phase1-sheet-changes.md`](./phase1-sheet-changes.md)。

| 列 | カラム名 | 型 | 説明 | 必須 |
|---|---|---|---|---|
| A | `案件ID` | 文字列 | 案件の一意 ID（例: `EST-20260309-001`） | ✓ |
| B | `見積日` | 日付 | `yyyy/MM/dd` | ✓ |
| C | `宛名` | 文字列 | 顧客名（例: 佐藤てるお） | ✓ |
| D | `敬称` | 文字列 | 様 / 御中 など | ✓ |
| E | `会社名` | 文字列 | 顧客の所属企業（個人なら空） | |
| F | `郵便番号` | 文字列 | ハイフン付き（例: `893-0025`） | |
| G | `住所` | 文字列 | 顧客住所 | |
| H | `TEL` | 文字列 | 電話番号 | |
| I | `担当者` | 文字列 | リフォレ側担当者（例: 梅下 生光） | |
| J | `備考` | 文字列 | 自由記述 | |
| K | `PDFファイル名` | 文字列 | 既存実装で使われている見積書 PDF のファイル名 | |
| L | `銀行名` | 文字列 | 売主の振込先銀行 | |
| M | `支店名` | 文字列 | 支店名 | |
| N | `種類` | 文字列 | 口座種別（普通 / 当座） | |
| O | `口座番号` | 文字列 | 口座番号 | |
| P | `口座名義` | 文字列 | 口座名義 | |
| Q | `見積金額(円)` | 数値/文字列 | 見積総額 | |
| R | `ステータス` | 文字列 | 案件のステータス（Phase 2 でプルダウン化） | |

#### Phase 1 で追加する列（S〜X）

| 列 | カラム名 | 型 | 説明 | 必須 |
|---|---|---|---|---|
| S | `契約日` | 日付 | 売買契約締結日 (yyyy/MM/dd)。GAS 側で和暦変換 | ✓ |
| T | `売主名義人` | 文字列 | 名義人が本人以外の場合のみ（Phase 2 で「売主名義人」「買主名義人」を別列化予定） | |
| U | `売主続柄` | 文字列 | 名義人との続柄 | |
| V | `手付金` | 数値 | 手付金額（円） | ✓ |
| W | `口座名義カナ` | 文字列 | 振込先名義のフリガナ | ✓ |
| X | `支払予定日` | 日付 | 代金支払予定日 (yyyy/MM/dd)。GAS 側で和暦変換 | ✓ |

### Phase 2 段階（案件マスタへの再設計）

Phase 2 着手時に **以下の列を追加・改名** する。順序は実装時に最適化するが、概念グループは下記の通り。

#### Group 1: 選択・識別系（行頭）

| カラム名 | 型 | 説明 |
|---|---|---|
| `選択` | チェックボックス | 「書類発行」メニュー実行時の対象選択。**列 A に配置** |
| `案件ID` | 文字列 | 既存 A 列を B 列にシフト |

#### Group 2: 案件基本情報

既存 B〜R 列を踏襲しつつ、以下を追加・分割:

| カラム名 | 型 | 説明 |
|---|---|---|
| `案件パターン` | プルダウン | `A①` / `A②` / `A③` / `B①` / `B②`。選択時に書類列の初期チェックを自動投入（onEdit） |
| `売主名義人` | 文字列 | A 系統では地主、B 系統ではリフォレ |
| `買主名義人` | 文字列 | A 系統ではリフォレ、B 系統では伐採業者など |
| `売主住所` | 文字列 | |
| `買主住所` | 文字列 | |
| `地域` | 文字列 | フォルダ名に使用（例: `鹿屋`, `南さつま`） |
| `担当営業` | 文字列 | リフォレ側担当者（既存「担当者」列を改名検討） |

#### Group 3: ステータス・進捗

| カラム名 | 型 | 説明 |
|---|---|---|
| `ステータス` | プルダウン | `未着手` / `出力済み` / `完了` / `保留` |
| `フォルダURL` | 文字列 | 案件フォルダへのリンク（GAS が自動セット） |
| `最終出力日時` | 日時 | 最後に書類発行された日時（GAS が自動セット） |

#### Group 4: 書類選択チェックボックス（14 列）

書類マスタシートの `書類ID` 順に列を立てる。各列はチェックボックス（true/false）。

| カラム名 | 説明 |
|---|---|
| `書類: 土地売買契約書` | A③ の標準 |
| `書類: 立木付土地売買契約書` | A② の標準 |
| `書類: 立木売買契約書` | A① の標準 |
| `書類: お客様契約書_土地込` | B② の標準 |
| `書類: お客様契約書_立木` | B① の標準 |
| `書類: 御見積書` | 見積段階の標準 |
| `書類: 請求書` | B 系統で発生 |
| `書類: 売上計算シート` | 社内 |
| `書類: チェックリスト` | 社内 |
| `書類: 法務局_X1` | 土地絡みで自動 ON（具体名は Phase 6 で詳細化） |
| `書類: 法務局_X2` | 同上 |
| `書類: 法務局_X3` | 同上 |
| `書類: 法務局_X4` | 同上 |
| `書類: 法務局_X5` | 同上 |

**初期チェック投入ロジック**（`onEdit` で実装）:
- `案件パターン` 列が編集されたら、対応する書類列を自動 ON
- 例: `A③` が選択されたら `書類: 土地売買契約書` と `書類: 法務局_X1..X5` を ON
- ユーザーは初期チェック後に個別調整できる（不要な書類を OFF にする / 追加で別書類を ON にする）

#### Group 5: 金額情報（既存列に加え集計用に整理）

| カラム名 | 説明 |
|---|---|
| `売上(立木)` | 立木契約金額（B 系統） |
| `売上(土地)` | 土地契約金額（B 系統） |
| `売上合計` | 立木 + 土地 |
| `仕入金額` | A 系統の仕入額 |
| `利益` | 売上 - 仕入 |
| `利益率` | 利益 / 売上 |
| `筆数` | 物件数（物件シートから集計、または手入力） |
| `面積(㎡)` | 全物件合計 |
| `雑抜き(㎡)` | 全物件合計 |

> Phase 2 着手時点で実カラムを最終確定。既存サンプルの分量に応じて、すぐ全列を入れるか段階的に追加するかを決める。

### 列構成サマリ（Phase 2 完了時の目安）

```
A: 選択☑
B: 案件ID
C: 案件パターン
D-K: 案件基本情報（宛名/敬称/住所/連絡先/担当営業 等）
L-Q: 売主買主情報（売主名義人/買主名義人/住所 等）
R-X: 契約・金額情報（契約日/手付金/口座/支払予定日 等）
Y-AE: 売上集計列（売上/仕入/利益/面積 等）
AF: ステータス▼
AG: フォルダURL
AH: 最終出力日時
AI-AV: 書類選択☑ × 14 書類
```

> 実列順は Phase 2 着手時に最終確定。視認性のため「よく見る列」を左寄せ、「集計列」「書類選択列」を右寄せにする方針。

---

## 3. 「物件」シート（旧「明細」シート）

案件 ID で紐づく繰返し項目。1 案件 = 複数行。

### Phase 1 で再設計

理由: 旧「住所（地目）」列が `南さつま市加世田内山田10647-1（立木）` のような形式で
所在・地番・地目（本来）が混在していたため、契約書差し込みのために分離する。

詳細は [`phase1-sheet-changes.md`](./phase1-sheet-changes.md) 参照。

### 新しい列構成

| 列 | カラム名 | 型 | 説明 |
|---|---|---|---|
| A | `案件ID` | 文字列 | 案件マスタの `案件ID` と結合 |
| B | `物件No` | 数値 | 案件内の連番（1, 2, 3...） |
| C | `所在` | 文字列 | 市区町村+大字（例: `南さつま市加世田内山田`） |
| D | `地番` | 文字列 | 地番（例: `10647-1`） |
| E | `地目` | 文字列 | 山林 / 田 / 畑 / 宅地 / 雑種地 等 |
| F | `面積(㎡)` | 数値 | 面積 |
| G | `雑抜き(㎡)` | 数値 | 雑抜き面積 |
| H | `樹種` | 文字列 | 杉 / 松 等 |
| I | `樹高(m)` | 文字列 | レンジ表記可（例: `20〜25m`） |
| J | `金額(円)` | 数値 | 当該物件の金額 |
| K | `備考` | 文字列 | 自由記述 |

### サンプル

```
案件ID            物件No  所在                    地番      地目  面積(㎡)  雑抜き(㎡)  樹種  樹高(m)    金額(円)
EST-20260309-001  1       南さつま市加世田内山田  10647-1   山林  7986      2833        杉    20〜25m    155000
EST-20260309-001  2       南さつま市加世田内山田  10647-1   山林              4918        松    15〜20m    175000
EST-20260309-001  3       南さつま市加世田内山田  10510-1   山林  2571      1190        杉    20〜25m    65000
```

旧形式での `〃` 省略は廃止。すべての行に所在を明記する。
「（立木）」は案件パターンで管理するため物件シートには入れない。

---

## 4. 「設定」シート

| 列 | カラム名 | 型 | 説明 |
|---|---|---|---|
| A | `項目` | 文字列 | 設定キー |
| B | `値` | 文字列 | 設定値 |
| C | `説明` | 文字列 | 補足 |

### 既存項目

| 項目 | 値 | 説明 |
|---|---|---|
| 出力先フォルダID | （空） | 空欄ならマイドライブ直下に保存（Phase 1 のみ） |
| テンプレートシート名 | 見積書 | 既存見積書ロジックが参照 |
| 案件一覧シート名 | 案件一覧 | Phase 2 で `案件マスタ` に改名予定 |
| 明細シート名 | 明細 | Phase 1 で `物件` に改名 |
| ボタン割当関数 | generateEstimatePdfFromActiveRow | 既存ボタン割当 |

### Phase 1 追加項目

| 項目 | 値 | 説明 |
|---|---|---|
| Phase1テンプレID_土地売買契約書 | （空） | プレースホルダ挿入済みコピーの Docs ID |
| Phase1出力先フォルダID | （空） | テストフォルダ `01_Phase1動作確認用` の ID |
| 物件シート名 | 物件 | Phase 1 で旧「明細」を改名 |

### Phase 2 追加予定項目

| 項目 | 値 | 説明 |
|---|---|---|
| 書類マスタシート名 | 書類マスタ | 書類設定の一覧（新設シート） |
| 案件マスタシート名 | 案件マスタ | Phase 2 で改名後 |
| 案件パターン列名 | 案件パターン | onEdit トリガーが判定に使う |
| 共通PDF出力先親フォルダID | （TBD） | `リフォレ案件/案件/` 相当の Drive フォルダ |
| バージョン区切り文字 | _v | 再出力時のファイル名 suffix（例: `_v2`） |

---

## 5. 「書類マスタ」シート（Phase 2 新設）

書類ごとの設定を 1 行 1 書類で保持する。

| 列 | カラム名 | 型 | 説明 |
|---|---|---|---|
| A | `書類ID` | 文字列 | 内部識別子（例: `purchase_contract_land`）。マスタの書類列名と紐付け |
| B | `書類名` | 文字列 | UI 表示名（例: 土地売買契約書） |
| C | `対応パターン` | 文字列 | カンマ区切り（例: `A③`, `A②,A③,B②`）。`onEdit` で初期チェック投入に使う |
| D | `テンプレートID` | 文字列 | Google Docs / Sheets の fileId |
| E | `テンプレ種別` | 文字列 | `docs` / `sheets` |
| F | `ファイル名テンプレ` | 文字列 | 命名規則（例: `{{案件ID}}_土地売買契約書_{{今日}}`）。`.pdf` は自動付与 |
| G | `明細含む` | 真偽 | 物件シートを表として差し込むか（Phase 3 で有効化） |
| H | `表示順` | 数値 | マスタシートでの書類列の並び順 |
| I | `備考` | 文字列 | |

> このシートは Phase 2 着手時にユーザーが手動で初期データを作成する。
> Phase 1 では使用しない（Phase 1 は土地売買契約書 1 書類だけハードコード）。

---

## 6. ステータスプルダウンの値

`案件マスタ` シートの `ステータス` 列に設定するプルダウン値:

| 値 | 意味 |
|---|---|
| `未着手` | 初期値 |
| `出力済み` | 「書類発行」メニュー実行直後（GAS が自動セット） |
| `完了` | 送付・契約締結等の最終状態（手動セット） |
| `保留` | 進行を止めている状態（手動セット） |

データ検証ルールで上記 4 値のみに制限。

---

## 7. 案件パターン → 書類セット 自動チェック表

`案件パターン` 列が選択されたとき、対応する書類列を自動 ON にする。

| パターン | 自動 ON する書類 |
|---|---|
| A① | 立木売買契約書 |
| A② | 立木付土地売買契約書, 法務局_X1〜X5 |
| A③ | 土地売買契約書, 法務局_X1〜X5 |
| B① | お客様契約書_立木, 御見積書, 請求書, 売上計算シート |
| B② | お客様契約書_土地込, 御見積書, 請求書, 売上計算シート, 法務局_X1〜X5 |

> パターン未選択の場合は自動 ON しない。ユーザーが個別チェックで対応。
> ユーザーが初期チェック後に手で OFF にしたものは尊重する（パターン再選択時に上書きしない）。
> ↑ この上書き挙動は Phase 2 着手時に確定。

---

## 8. 確認・確定が必要な項目（Phase 1〜2 にまたがる）

- [ ] **案件マスタの最終列構成**: Phase 2 で実カラムを確定（特に売上・仕入金額の細目）
- [ ] **ファイル名規則**: `{案件ID}_{書類名}_{日付}.pdf` で問題ないか、再出力時の `_v2` 表記の位置
- [ ] **乙（リフォレ側）情報の管理**:
  - 現状はテンプレに固定で書かれている（住所・代表者名・TEL 等）
  - 設定シートに切り出して動的化するか？（担当者の変更があり得るため検討）
- [ ] **金額の和暦/カンマ表記**: 契約書では「金五十万円」のような書式が必要か
- [ ] **書類マスタの法務局 5 書類**: 具体的な書類名・対応パターンを Phase 6 着手前に確定
- [ ] **パターン再選択時の挙動**: 既存の手動チェックを上書きするか、ユーザーに確認ダイアログを出すか
