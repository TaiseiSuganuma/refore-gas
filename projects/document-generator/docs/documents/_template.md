# <書類名>

> このファイルは新しい書類を追加する際の **テンプレート** です。
> コピーして `<書類ID>.md` にリネームしてから埋めてください。

## 基本情報

| 項目 | 値 |
|---|---|
| 書類ID | `<書類ID>`（snake_case） |
| 書類名（UI 表示） | <書類名> |
| 用途 / 役割 | この書類が業務上どんな意味を持つか |
| テンプレート Docs ID | `<Google Docs fileId>` |
| 出力先フォルダ ID | `<Drive folder ID>`（案件ごとサブフォルダなら親 ID） |
| 出力ファイル名 | `<命名規則>` |

## 出力条件

この書類を出力するための条件（マスタシートの値による分岐）。

例:
- 売買金額 > 0
- 売主種別 == "個人"
- 仲介手数料区分 == "両手"

## 使用するプレースホルダ

| プレースホルダ | マスタの参照元 | フォーマット | 必須 | 備考 |
|---|---|---|---|---|
| `{{customer_name}}` | 案件マスタ「顧客名」列 | そのまま | ✓ | |
| `{{contract_date:date_jp}}` | 案件マスタ「契約日」列 | 和暦 | ✓ | |
| ... | | | | |

## 条件分岐・繰返し（該当する場合のみ）

- 条件分岐: `{{#if is_corporate}}…{{/if}}` のような箇所と、それぞれの分岐内容
- 繰返し: `{{#each guarantors}}…{{/each}}` のリスト構造

## 既知の制約・特記事項

- このテンプレ特有の注意点
- 過去に発生したトラブル / 注意した方が良い書式

## 作業履歴

- YYYY-MM-DD: 初版作成
