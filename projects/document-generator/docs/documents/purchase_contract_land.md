# 土地売買契約書（purchase_contract_land）

## 基本情報

| 項目 | 値 |
|---|---|
| 書類ID | `purchase_contract_land` |
| 書類名（UI 表示） | 土地売買契約書 |
| 系統 | **A③**（仕入: 売主 → リフォレ、土地のみ） |
| 用途 / 役割 | 売主から土地のみを仕入れる際の売買契約書（A 系統で最もシンプルなケース） |
| テンプレート Docs ID | `1AsZLK9_rmIF-VC62I42QMAmPmzNz0kp1KmegOZ6ApJI` |
| テンプレ種別 | Google Docs |
| 出力先フォルダ ID | TBD（案件サブフォルダを自動作成する想定） |
| 出力ファイル名 | `{{案件ID}}_土地売買契約書_{{今日}}.pdf` |
| 明細繰返し | **なし** |
| Phase | **1（MVP 対象）** |

## 出力条件

- 案件のパターンが **A③（土地のみ）** の場合に出力対象となる
- A② / A③ が混在するケースの扱いは要検討（書類マスタの「系統」列で判定）

## 使用するプレースホルダ

> ⚠️ テンプレ実物には現状 `____` の手書き穴埋めしか入っていない。
> Phase 1 着手時に以下のプレースホルダ挿入作業をテンプレに対して行う必要がある。

### 第１条（本件土地の売買）

| プレースホルダ | マスタの参照元 | フォーマット | 必須 |
|---|---|---|---|
| `{{所在}}` | 案件一覧「住所」または明細から導出（要検討） | そのまま | ✓ |
| `{{地番}}` | TBD（案件一覧に列が無い、登記情報から?） | そのまま | ✓ |
| `{{地目}}` | TBD | そのまま | ✓ |
| `{{筆数}}` | TBD | 数値 | ✓ |
| `{{地積}}` | 明細「面積(㎡)」を合算？ | 数値 + 単位 | ✓ |
| `{{土地合計}}` | 明細「面積(㎡)」の合計 | 数値 | ✓ |
| `{{売買金額}}` | 案件一覧「見積金額(円)」 | カンマ区切り | ✓ |

### 第２条（手付金）

| プレースホルダ | マスタの参照元 | フォーマット | 必須 |
|---|---|---|---|
| `{{手付金}}` | TBD（案件一覧に列無し、要追加） | カンマ区切り | ✓ |
| `{{銀行名}}` | 案件一覧「銀行名」 | そのまま | ✓ |
| `{{支店名}}` | 案件一覧「支店名」 | そのまま | ✓ |
| `{{口座種類}}` | 案件一覧「種類」 | そのまま | ✓ |
| `{{口座番号}}` | 案件一覧「口座番号」 | そのまま | ✓ |
| `{{口座名義}}` | 案件一覧「口座名義」 | そのまま | ✓ |
| `{{口座名義カナ}}` | TBD（必要なら追加列） | カタカナ | |

### 第３条（代金の支払い）

| プレースホルダ | マスタの参照元 | フォーマット | 必須 |
|---|---|---|---|
| `{{支払予定日}}` | TBD | 和暦 or 西暦 | ✓ |

### 契約締結日と甲（売主）情報

| プレースホルダ | マスタの参照元 | フォーマット | 必須 |
|---|---|---|---|
| `{{契約日}}` | TBD（生成日 or 別途指定） | 和暦 | ✓ |
| `{{売主名義人}}` | 案件一覧「宛名」 | そのまま | ✓ |
| `{{売主続柄}}` | TBD（本人なら空） | そのまま | |
| `{{売主住所}}` | 案件一覧「住所」 | そのまま | ✓ |
| `{{売主氏名}}` | 案件一覧「宛名」 | そのまま | ✓ |
| `{{売主連絡先}}` | 案件一覧「TEL」 | そのまま | ✓ |

### 乙（リフォレ）情報

現状テンプレに固定で書かれている:
- 住所: 鹿児島県鹿屋市西祓川町509番地5
- 株式会社リフォレ 代表取締役 川林 雄城
- TEL 0994-36-8692 / FAX 0994-36-8693
- 営業担当 梅下 生光 / 連絡先 080-8570-3872

これらを動的化するかは要検討（担当者は変わる可能性がある）。
Phase 1 では固定のままで進める方向。

## 条件分岐・繰返し

- なし（Phase 1 対象書類として選定の理由）

## 既知の制約・特記事項

### テンプレ準備が必須

- 現状のテンプレには **プレースホルダがまだ入っていない**（`____` のみ）
- Phase 1 着手時、ユーザーがテンプレに `{{key}}` を挿入する必要がある
- テンプレ編集後はコピーを取り、原本に手を加える前に動作確認する

### マスタシートに不足する情報

- 第１条の「地番」「地目」「筆数」 → 案件一覧に該当列が無い。登記情報を別途取り込むか、入力列を追加するか要決定
- 第２条「手付金」 → 案件一覧に列無し（金額の一部として手付金がいくらかは別管理が必要）
- 第３条「支払予定日」 → 入力列を追加するか、ダイアログで入力させるか

### Docs の `replaceText` 注意点

- Google Docs は文字単位で書式が異なると `body.replaceText("{{key}}", ...)` がマッチしない
- テンプレ作成時、プレースホルダは **同じフォント・書式で一気に入力** する

## 作業履歴

- 2026-05-17: 初版作成（テンプレ実物確認・Phase 1 MVP 対象として選定）
